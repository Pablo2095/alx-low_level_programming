#include <stdio.h>
/**
 * main - Entry point.
 * 
 * Return: Always 0 (Success)
 */
int main(void)
{
        char b;

	char s;

	b = 'a';
	s = 'A';
        while
		(b <= 'k'){
			 putchar(b);
			 b++;
		} 
	while
		(s <= 'K'){
			putchar(s);
			s++;
		}
	putchar('\n');
        return (0);
}
