#include "main.h"
/**
 * mul - multiplies two integers
 * @p: The first number is multiplication
 * @s: The second number to be mulyiplied the  first number
 * Return: The results of the multiplication between p and s
 */
int mul(int p, int s)

{
	return (p * s);
}
